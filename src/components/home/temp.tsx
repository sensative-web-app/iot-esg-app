import { SessionData } from "@/lib/session";
// import { getTemp } from "@/actions";

export const Temperature = async ({ session }: { session: SessionData }) => {
  // const temp = await getTemp(id, measurement);

  return <div className="pt-8"></div>;
};
